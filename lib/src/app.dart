import 'package:flutter/material.dart';
import 'package:flutter_practica2/src/pages/contador_page.dart';
class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        home: Center(
          child: ContadorPage(),
        ));
  }
}
